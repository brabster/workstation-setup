---
# tasks file for dev
- name: Configure file watchers for IDEs, build tooling
  become: yes
  sysctl:
    name: fs.inotify.max_user_watches
    value: '528288'
    state: present
- name: Setup git username and email
  copy: src=.gitconfig dest="/home/paul/.gitconfig"
